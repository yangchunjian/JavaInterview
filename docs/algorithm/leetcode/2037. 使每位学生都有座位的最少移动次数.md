---
categories: 
  - algorithm
  - leetcode
tags: 
  - Java
author: 
  name: JavaInterview.cn
  link: https://JavaInterview.cn
titleTag: Java
feed: 
  enable: true
description: 将座位和学生位置分别排序后按序配对，累加绝对差即为最少移动次数。
title: 2037. 使每位学生都有座位的最少移动次数
---

## 题目

一个房间里有 `n` 个 **空闲** 座位和 `n` 名 **站着的** 学生，房间用一个数轴表示。给你一个长度为 `n` 的数组 `seats` ，其中 `seats[i]` 是第 `i` 个座位的位置。同时给你一个长度为 `n` 的数组 `students` ，其中 `students[j]` 是第 `j` 位学生的位置。

你可以执行以下操作任意次：

- 增加或者减少第 `i` 位学生的位置，每次变化量为 `1` （也就是将第 `i` 位学生从位置 `x` 移动到 `x + 1` 或者 `x - 1`）

请你返回使所有学生都有座位坐的 **最少移动次数** ，并确保没有两位学生的座位相同。

请注意，初始时有可能有多个座位或者多位学生在 **同一** 位置。

### 示例 1

| 项目 | 内容 |
| --- | --- |
| **输入** | `seats = [3,1,5]`, `students = [2,7,4]` |
| **输出** | `4` |

**解释：** 学生移动方式如下：

- 第一位学生从位置 2 移动到位置 1 ，移动 1 次。
- 第二位学生从位置 7 移动到位置 5 ，移动 2 次。
- 第三位学生从位置 4 移动到位置 3 ，移动 1 次。

总共 1 + 2 + 1 = 4 次移动。

### 示例 2

| 项目 | 内容 |
| --- | --- |
| **输入** | `seats = [4,1,5,9]`, `students = [1,3,2,6]` |
| **输出** | `7` |

**解释：** 学生移动方式如下：

- 第一位学生不移动。
- 第二位学生从位置 3 移动到位置 4 ，移动 1 次。
- 第三位学生从位置 2 移动到位置 5 ，移动 3 次。
- 第四位学生从位置 6 移动到位置 9 ，移动 3 次。

总共 0 + 1 + 3 + 3 = 7 次移动。

### 示例 3

| 项目 | 内容 |
| --- | --- |
| **输入** | `seats = [2,2,6,6]`, `students = [1,3,2,6]` |
| **输出** | `4` |

**解释：** 学生移动方式如下：

- 第一位学生从位置 1 移动到位置 2 ，移动 1 次。
- 第二位学生从位置 3 移动到位置 6 ，移动 3 次。
- 第三位学生不移动。
- 第四位学生不移动。

总共 1 + 3 + 0 + 0 = 4 次移动。

### 提示

- `n == seats.length == students.length`
- `1 <= n <= 100`
- `1 <= seats[i], students[j] <= 100`

## 思路

座位和学生都在数轴上，一次操作只能把某位学生的位置加 1 或减 1，目标是让每位学生坐到不同座位，并使总移动次数最少。一位学生从位置 a 走到座位 b，代价就是 a 与 b 差的绝对值。

关键在于怎么配对。若把学生按位置从小到大排，座位也按位置从小到大排，则第 i 小的学生应对到第 i 小的座位。反证：若出现交叉配对（左边学生去更靠右的座位、右边学生去更靠左的座位），交换这两次分配后总路程不会变大，因此最优方案一定可以整理成「按序配对」。

于是做法是：对 `seats` 和 `students` 分别排序，再对每个下标累加 `Math.abs(seats[i] - students[i])`。n 最大为 100，排序即可。位置范围也只有 1 到 100，计数排序同样可行，但直接调用 `Arrays.sort` 更直观。

注意初始时多个座位或多个学生可以在同一位置，排序后仍按个数一一对应即可，不必额外去重。

## 解法

```java
class Solution {
    public int minMovesToSeat(int[] seats, int[] students) {
        Arrays.sort(seats);
        Arrays.sort(students);
        int ans = 0;
        for (int i = 0; i < seats.length; i++) {
            ans += Math.abs(seats[i] - students[i]);
        }
        return ans;
    }
}
```

## 总结

- 最少移动次数等于按位置排序后，第 i 个学生走到第 i 个座位的距离之和。
- 交叉分配不会更优，因此排序后按序配对就是贪心最优。
- 同一位置可以有多个座位或学生，按出现次数配对，不要去重。
